<div class="col-sm-12">

    <p>På mine sider kan du enkelt se hvilken data som er registrert på deg hos {{ site_name }}.
    Her vil du også kunne endre data som er registrert på deg. Dette kan du endre ved å benytte linkene i
    venstre meny. Eventuelle endringer du gjør vil ikke være synlig før du oppdaterer siden.</p>
    <br>
    <p>Følgende data er registrert:</p>
    {%  if user.role == 1 %}
    <div class="col-sm-8 col-sm-offset-2">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <div class="row">
                    <p style="text-align: center">Du har skrevet {{ numberOfComments }} kommentarer</p>
                </div>
            </div>
        </div>
    </div>
    {%  elseif user.role == 2 %}
        <div class="row">
            <div class="col-sm-6">
                <div class="panel panel-info">
                    <!-- Default panel contents -->
                    <div class="panel-heading">Antall poster av deg</div>
                    <div class="panel-body">
                        <p>{{ numberOfPosts }}</p>
                    </div>
                </div>
            </div>

            <div class="col-sm-6">
                <div class="panel panel-info">
                    <!-- Default panel contents -->
                    <div class="panel-heading">Antall kommentarer av deg</div>
                    <div class="panel-body">
                        <p>{{ numberOfComments }}</p>
                    </div>
                </div>
            </div>

        </div>
    {% endif %}

    <table class="table table-hover">
        <thead>
        <tr>
            <th>Hva</th>
            <th>Registrert</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>Brukernavn:</td>
            <td>{{ user.username }}</td>
        </tr>
        <tr>
            <td>Fornavn:</td>
            <td>{{ user.firstname }}</td>
        </tr>
        <tr>
            <td>Etternavn:</td>
            <td>{{ user.lastname }}</td>
        </tr>
        <tr>
            <td>Epost:</td>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <td>Tilgang:</td>
            <td>{% if user.role == 2 %}
                    Administrator
                {% elseif user.role == 1 %}
                    Ordinær bruker
                {% endif %}

            </td>
        </tr>
        <tr>
            <td>Siste besøk:</td>
            <td>{{ user.lastlogin | date('d/m/Y H:i:s')}}</td>
        </tr>
        <tr>
            <td>Registrert siden:</td>
            <td>{{ user.regdate | date('d/m/Y H:i:s') }}</td>
        </tr>
        </tbody>

    </table>



</div>


